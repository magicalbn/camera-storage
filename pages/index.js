import React from "react";
import Camera from "../components/Camera";

const Home = () => {
    return (
        <div className="bg-black h-screen w-screen">
            <Camera />
        </div>
    );
};

export default Home;
